{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter @workspace/api-server --filter @workspace/preventivo-ai --filter @workspace/scripts --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "lint": "eslint . --max-warnings=1000",
    "lint:strict": "eslint . --max-warnings=0",
    "knip": "knip --no-progress",
    "env:inventory": "pnpm --filter @workspace/scripts env-inventory",
    "spell": "cspell --no-progress artifacts/preventivo-ai/src/i18n/*.ts artifacts/preventivo-ai/src/data/*.ts artifacts/preventivo-ai/src/pages/*.tsx artifacts/api-server/src/lib/email*.ts artifacts/api-server/src/lib/*Messaging.ts"
  },
  "private": true,
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "cspell": "^10.3.3",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.12.0",
    "knip": "^6.36.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2",
    "typescript-eslint": "^8.70.0",
    "@cspell/dict-it-it": "^3.1.3"
  },
  "optionalDependencies": {
    "@esbuild/win32-x64": "0.27.3"
  },
  "packageManager": "pnpm@11.8.0"
}
