# PrevAI — Piano d'azione complessivo

**Creato:** 2026-09-21. **Questo è il file da aprire all'inizio di ogni sessione.** Contiene la sequenza di tutte le fasi, lo stato di avanzamento e il punto esatto da cui ripartire. I dettagli di ogni fase sono nei due piani figli:
- `PREVAI-V2-PLAN.md` — porting del codebase QuoteAI in PrevAI (fasi **V2-0 → V2-6**)
- `AMMINISTRAZIONE-PLAN.md` — fatturazione SDI, motore fiscale forfettari, amministrazione d'impresa (fasi **A-0 → A-6**)

## Regole di lavoro (valgono per ogni sessione)
1. **Una fase alla volta, un agente alla volta.** Niente agenti in parallelo.
2. Ogni fase si chiude con: typecheck pulito, build verde, test verdi, **un commit** con messaggio `V2-N: …` / `A-N: …`, e **aggiornamento della tabella di stato qui sotto** (stato, data, commit, note).
3. **Dati di produzione**: valgono le 8 regole di `PREVAI-V2-PLAN.md` §1 in ogni fase, anche quelle del modulo Amministrazione. Mai `drizzle-kit push` sulla prod. Solo SQL additivo. Dump prima di ogni migrazione.
4. **Non toccare il repo QuoteAI** (`C:\Users\Admin\OneDrive\Desktop\QuoteAI`): è riferimento in sola lettura.
5. Se una fase scopre qualcosa che cambia il piano, si aggiorna il piano figlio **prima** di continuare, e si annota qui nelle note.
6. Le decisioni che spettano al titolare sono elencate in §"Decisioni aperte": non si inventano, si chiedono.

## Sequenza e stato

| # | Fase | Piano | Sforzo | Stato | Data | Commit | Note |
|---|---|---|---|---|---|---|---|
| 1 | **V2-0** Sicurezza e igiene: revoca PAT, remote puliti, triage 101 file non committati, `pg_dump` baseline, inventario env, tag `v1-final`, branch `v2` | V2 §4 | ½ g | ✅ fatto | 2026-09-21 | `6dbe45de4` (main) | main pulito (7 commit), dump baseline OK, tag `v1-final`, branch `v2` |
| 2 | **V2-1** Import base QuoteAI nel branch `v2`, rinomina `quote-ai` → `preventivo-ai`, CI e docs | V2 §4 | 1 g | ✅ fatto | 2026-09-21 | commit `V2-1: import base QuoteAI…` su `v2` | base = QuoteAI `5d65f61` (Phase 71); typecheck, build, 65 test verdi; `.gitattributes` LF |
| 3 | **V2-2** Ri-italianizzazione: `@workspace/config`, locale solo `it`, slug SEO v1 identici, blog v1, prompt AI unificato | V2 §4 | 5–7 g | ✅ fatto | 2026-09-21 | `52c1e6fb7` … `09bd1c7df` + chiusura su `v2` (a–f, 10 commit) | Sotto-tappe: **a** fondamenta ✅ · **b** SEO v1 ✅ · **c** `translations.ts` + pagine pubbliche ✅ · **d** `translations.dashboard.ts` ✅ · **e** prompt AI unificato + WhatsApp/notifiche/piani ✅ · **f** audit CI invertito ✅. Restano per V2-3: dati e2e canadesi (ON/QC) nell'harness `api-server/src/e2e`; testi delle integrazioni disattivate (QuickBooks/Wave/Flinks/LSA) decisi in V2-4 |
| 4 | **V2-3** Prova generale su staging da dump reale; migrazioni additive; v1 e v2 entrambe funzionanti sul DB migrato | V2 §4 | 2–3 g | ✅ fatto | 2026-09-21 | `a638af781` (v2) | Staging = **Postgres 17 locale** (decisione titolare, niente progetto Supabase). Migrazione `migrations/v2/0001` additiva, 342 ms, idempotente; drift 0; e2e 63/63 (harness italianizzato, storage in memoria); v1 e v2 verificate sul DB migrato. Da rigenerare in V2-5 dallo schema finale |
| 5 | **V2-4** Riconciliazione feature per l'Italia (tabella keep/swap/disable; incentivi v1 ripristinati; fatture pro-forma) | V2 §4 | 5–8 g | ✅ fatto | 2026-09-22 | `40f767c1a` … `59a5f0215` + chiusura su `v2` (a, b/d, c, e, f) | D3 confermata. **a** QuickBooks/Wave/Flinks/Financeit/Google LSA rimossi (codice, schema, OpenAPI, UI, e2e) · **b** profilo fiscale IT (`codice_fiscale`, `codice_sdi`, `rea_number`, `iban`, `secondary_review_url`; colonne canadesi via) · **c** incentivi v1 (schema prod, endpoint widget, calcolo a parità, PDF, admin) · **d** fatture emesse come pro-forma PF-/NC- con avviso art. 21 · **e** firmatario con C.F./P. IVA, testi eIDAS/CAD, GDPR, prompt senza segnaposto. Gating piani: team/cantieri/pro-forma da Pro (già così in `plans.ts`). Staging: delta `migrations/v2/0002` (da fondere in 0001 in V2-5), drift 0, e2e 60/60 |
| 6 | **V2-5** Migrazione produzione + cutover su Vercel `prevai` (preview → promote), rollback pre-scritto | V2 §4 | 1 g + 48 h monitoraggio | 🟨 in corso | 2026-09-21 | `32668e401` (V2-5a) su `v2` | **← PROSSIMA (cutover)**. **a** ✅ preparazione: `migrations/v2/0001` rigenerata dallo schema finale (43 tabelle, 53 colonne, 45 FK, 69 indici; 409 ms; schema = staging V2-4; drift 0; e2e 60/60), `0002` eliminata, runbook cutover+rollback in RUNBOOKS §5, `docs/sql/reconcile.sql`. **b** ⬜ env Vercel (§5.1) + push `v2` + smoke preview (§5.2) — richiede D2 e ok del titolare. **c** ⬜ finestra: dump, migrazione prod, smoke v1/v2, promote, 48 h (§5.3) |
| 7 | **V2-6** Consolidamento: **AI Act art. 50** (scadenza 2/12/2026), suite QA, SEO check, poi `v2` → `main` | V2 §4 | 1–2 sett. | 🟨 pre-cutover fatto | 2026-09-22 |  `eb5ba0cd0` su `v2` | **a** ✅ AI Act art. 50: avviso IA al primo contatto (support bot, assistente cantiere, bot WhatsApp), metadati PDF `Subject`/`Keywords` (`pdfInfo()` in `lib/pdfmake.ts`: preventivi = AI-generated, contratti = AI-assisted, fatture = nessuna), riga a piè di pagina sui preventivi, avviso nell'email e nella pagina pubblica `/p/:id`. **b** ✅ suite QA su `v2`: unit 72, e2e 60/60, `qa:pdf` 44/44 con nuovo lint (residui inglesi + marcatura IA), `qa:visual` 104 pagine axe 0 serious/critical, Lighthouse CLS 0 ovunque, i18n-audit 0, cspell 0. **c** ✅ SEO: 330/330 URL v1 prerenderizzati con canonical identico e hreflang solo `it-IT`+`x-default`. **d** ⬜ dopo V2-5: copertura GSC, `v2` → `main`, fase contract solo dopo ≥1 ciclo di fatturazione |
| 8 | **A-0** Compliance e fondamenta: DPIA, DPA, 2FA obbligatoria sul modulo, cifratura campi fiscali, revisione regole 2026 con commercialista | A §10 | 1 sett. | 🟨 fatto tranne D6 | 2026-09-21 | `e1d12efe5` su `v2` | Eseguita **prima** del cutover (D2 ancora aperta), su `v2` come migrazione additiva `0002_a0_compliance.sql` (una colonna). **Fatto**: policy org `two_factor_required` (owner-only, gate 403 `two_factor_required` in `requireAuth`, schermata di blocco in dashboard, e2e 6/6); cifratura a riposo di `business_profiles.iban` (`fieldCrypto.ts`, `enc1:`, lettura trasparente del pregresso, `ops:encrypt-fiscal-fields`, rotazione chiave estesa); accordo art. 28 utente↔PrevAI nei Termini §9 + trasparenza IA §7 + privacy §2/§8; `docs/compliance/`: `DPIA.md`, `REGISTRO-TRATTAMENTI.md`, `RESPONSABILI-ART28.md`, `REVISIONE-COMMERCIALISTA.md`; RUNBOOKS §5.3 punto 11, §5.5, §5.6. **Resta al titolare**: D6 (commercialista → checklist F1–F16 e 5 casi golden già pronti), DPA fornitori (tabella in `RESPONSABILI-ART28.md`, priorità Groq), dati societari e firma in `DPIA.md` §1/§8, formazione IA art. 4. Cutover: eseguire 0002 dopo 0001, poi `ops:encrypt-fiscal-fields` dopo il promote |
| 9 | **A-1** Fatture SDI via intermediario (Openapi default), onboarding delega, XML FatturaPA, stati SdI, conservazione, bollo, ciclo passivo | A §10 | 2–3 sett. | 🟨 fatto tranne D8 | 2026-09-22 | `9d5ada039`…`fe34dc0df` su `v2` | Codice completo e verde su staging; **nessuna impresa può emettere finché il titolare non firma contratto e DPA con l'intermediario (D8)**: senza credenziali il modulo resta sul provider `simulato`, che non trasmette nulla. **Fatto**: codifiche FatturaPA in `@workspace/config` (tipi documento, regimi, nature, controlli su P. IVA/C.F./codice destinatario/IBAN, dizionario degli errori di scarto); generatore XML FatturaPA 1.2.2 + 25 controlli pre-invio, ciascuno col codice di scarto che evita; adapter intermediario (interfaccia unica anti lock-in, `simulato` per dev/staging/e2e, `openapi` completo ma inerte senza token); invio esplicito con XML archiviato anche da noi, stati SdI via webhook + polling nel cron, notifiche per scarto/mancata consegna/rifiuto, rinvio dopo scarto; **serie FT- e `invoices.fiscale`**: col modulo attivo il documento è una fattura ai sensi dell'art. 21 e il PDF diventa copia di cortesia (pro-forma per tutti gli altri); bollo virtuale ricalcolato per trimestre con F24 precompilato (codici 2521-2524); ciclo passivo solo su adesione esplicita (Garante 2018-2019) con collegamento manuale a cantiere → costo; add-on `sdi_invoicing` (nessun piano lo include) + 2FA obbligatoria fra i requisiti; UI: pannello sulla fattura, scheda Impostazioni, pagina Amministrazione. Migrazione `migrations/v2/0003_a1_sdi.sql` additiva (dopo la 0002), applicata su staging: drift 0, **e2e 79/79**, unit 79, lint 0 errori, build verde. Runbook operativo in RUNBOOKS §6 |
| 10 | **A-2** Motore fiscale forfettario versionato, onboarding fiscale, "quanto mettere via", monitor soglia con pipeline, simulatore | A §10 | 2 sett. | 🟨 fatto tranne D6 | 2026-09-22 | `0f441fd06`…`6de1c9139` su `v2` | Codice completo e verde; **nessun numero è verificato finché il titolare non porta le regole a un commercialista (D6)**, e il prodotto lo dichiara in ogni schermata e in ogni risposta API. **Fatto**: motore puro versionato per anno d'imposta in `lib/config/src/fiscale/` (`regole/2026.ts` con le 16 regole F1–F16, ciascuna con fonte e `revisione.stato`; `ateco.ts` con il coefficiente per gruppo, 86 % per le costruzioni; `calcolo.ts`; `golden.ts` con i 5 casi della checklist); nessun importo esce senza la sua `Spiegazione` (formula, passaggi, fonte, regole); tabelle `tax_profiles` e `fiscal_payments` (`migrations/v2/0004_a2_fiscale.sql`, additiva, dopo la 0003); raccolta dati **per cassa** da `invoice_payments` (incassi), fatture aperte (maturato) e preventivi accettati (pipeline); monitor della soglia nel cron che avvisa una volta sola quando il livello peggiora; simulatore "se accetto questo lavoro"; pagina `/dashboard/fisco` con onboarding, calcolo, soglia, scadenze, requisiti, simulatore e registro versamenti; add-on `fiscal_engine` (nessun piano lo include, separato da `sdi_invoicing`). **Nuova area di permessi chiusa** `fiscale`: owner full, admin view, office/foreman/viewer niente — la posizione fiscale del titolare non è un dato di lavoro. Staging: migrazione 303 ms, drift 0, **e2e 92/92**, unit 120, lint 0 errori, i18n-audit 0, cspell 0, build verde, pagina provata a video contro lo staging. Runbook in RUNBOOKS §7 |
| 11 | **A-3** Scadenzario + F24 precompilati + promemoria | A §10 | 1 sett. | ⬜ | | | |
| 12 | **A-4** Prima nota, import estratto conto, utile netto dopo tasse, chiusura d'anno, "Condividi col commercialista" | A §10 | 2 sett. | ⬜ | | | |
| 13 | **A-5** Pricing add-on su Stripe, paywall, landing con test prezzo, SEO | A §10 | 1 sett. | ⬜ | | | richiede D5 |
| 14 | **A-6** Commercialista nel loop (fase 2): convenzione, incarico digitale, revisione→invio, consulenza, L. 132/2025 | A §10 | 3–4 sett. + negoziazione | ⬜ | | | decisione separata, D9 |
| — | Dopo: contract phase (drop colonne vecchie), PSD2, regime semplificato, SDI export avanzato | V2 §4 V2-6 / A §10 | — | ⬜ | | | piani separati |

Legenda stato: ⬜ da fare · 🟨 in corso · ✅ fatto · ⛔ bloccato (scrivere perché nelle note)

**Stima totale:** v2 al cutover ~3–4 settimane; Amministrazione fase 1 ~9–11 settimane dopo; fase 2 a seguire.

## Decisioni aperte (spettano al titolare)

| ID | Decisione | Serve entro | Stato |
|---|---|---|---|
| D0 | **Revocare il PAT GitHub** presente nell'URL del remote `origin` del clone PrevAI (GitHub → Settings → Developer settings → Personal access tokens). Azione sull'account, non delegabile | V2-0 | ✅ 2026-09-21 — revocato dal titolare; remote `origin` ripulito, 25 remote Replit + gitsafe rimossi |
| D1 | Design: prevai.it adotta il design system navy/Figtree di QuoteAI così com'è, o si preservano logo/colori attuali PrevAI? | V2-2 | ✅ 2026-09-21 — **design QuoteAI così com'è** (homepage, dashboard, tutto), per ora; solo brand/testi cambiano |
| D2 | Finestra di manutenzione per il cutover (proposta: mattina presto, giorno feriale, ora italiana, es. martedì 07:00–08:00; la migrazione dura < 1 s e v1 resta live, l'unico momento visibile è il promote) | V2-5 | ⬜ |
| D3 | Conferma tabella V2-4: fatture come pro-forma finché non c'è SDI; incentivi v1 ripristinati; integrazioni canadesi disattivate | V2-4 | ✅ 2026-09-22 — confermata così com'è (tabelle delle integrazioni rimosse dallo schema); gating piani: team, cantieri e pro-forma da Pro in su |
| D4 | Cosa fare dei 101 file non committati su `main` (si decide in V2-0 guardandoli insieme) | V2-0 | ✅ 2026-09-21 — 4 feature committate per tema (accettazione pubblica, OCR listino, home CRM, hero SEO), 88 OG rigenerate scartate, 2 file spuri spostati in Downloads |
| D5 | Nome del modulo ("PrevAI Amministrazione" placeholder) e ok ai prezzi ipotesi: gratis calcolatore/scadenzario; add-on 12 €/mese o 120 €/anno; fase 2 ~420 €/anno IVA incl. | A-5 | ⬜ |
| D6 | Commercialista partner per revisionare il motore regole 2026 (di fiducia o da cercare) | A-0 | ⬜ — pacchetto pronto in `docs/compliance/REVISIONE-COMMERCIALISTA.md` (16 regole + 5 casi golden). **Aggiornato 2026-09-22**: A-2 è stata eseguita lo stesso, con le regole codificate ma tutte marcate `non_revisionata` e il modulo dietro un flag che nessun piano include — come A-0 con D2 e A-1 con D8. Alla riunione si arriva coi nostri valori già calcolati accanto a ogni caso golden. **Nessuna impresa può usare questi numeri per versare finché la checklist non è firmata**; la procedura per riportare le risposte nel codice è in fondo a quel file e in RUNBOOKS §7.1 |
| D7 | Regione UE del progetto Supabase PrevAI (verificare; se non UE, valutare) | A-0 | ✅ verificato 2026-09-21: `aws-0-eu-west-1` (Irlanda, UE) |
| D8 | Intermediario SDI: partire con Openapi.it (prezzi pubblici, no setup) + preventivo A-Cube. Contratto e DPA da firmare dal titolare | A-1 | ⬜ — 2026-09-22: il titolare ha confermato **Openapi come default** e la struttura adapter + simulatore; restano da firmare contratto, DPA e manuale di conservazione, poi basta incollare il token in Impostazioni → Fatture elettroniche. Da riconfrontare, all'attivazione, le rotte in `sdi/providers/openapi.ts` con la documentazione del proprio account |
| D9 | Fase 2: unico studio partner vs rete di professionisti convenzionati | A-6 | ⬜ |

## Come iniziare la prossima sessione (V2-5 b/c: cutover)

> Aggiornamento 2026-09-22: la parte pre-cutover di V2-6 (AI Act, suite QA, SEO) è già fatta su `v2` (riga 7 🟨). Dopo il cutover restano solo GSC, `v2` → `main` e, molto più avanti, la fase contract.
> Aggiornamento A-2 (2026-09-22): anche A-2 è su `v2` (riga 10 🟨). Il cutover esegue ora **quattro** migrazioni, in ordine: 0001, `0002_a0_compliance.sql`, `0003_a1_sdi.sql`, `0004_a2_fiscale.sql`. Nessuna env nuova. Le due tabelle di A-2 nascono vuote e il modulo è spento per tutti finché non si accende il flag `fiscal_engine` — che, finché D6 è aperta, non va acceso su nessuno che debba versare davvero.
>
> Aggiornamento A-1 (2026-09-22): anche A-1 è su `v2` (riga 9 🟨). Nessuna env nuova è obbligatoria (le credenziali dell'intermediario stanno cifrate in tabella, per impresa); il modulo è spento per tutti finché non si accende il flag `sdi_invoicing`.
>
> Aggiornamento A-0 (2026-09-21): anche A-0 è su `v2` (riga 8 🟨). Il cutover ora esegue **due** migrazioni (0001 poi `0002_a0_compliance.sql`) e, **dopo il promote**, `ops:encrypt-fiscal-fields --apply` (RUNBOOKS §5.3 punto 11, §5.5). Le env Vercel non cambiano (`TOKEN_ENCRYPTION_KEY` esiste già).

Apri una sessione nella cartella `C:\Users\Admin\Downloads\PrevAI (2)\PrevAI`, `git checkout v2`, e scrivi: **"Leggi docs/PIANO-AZIONE.md ed esegui la fase V2-5"**. Il runbook completo è `RUNBOOKS.md` **§5** (env 5.1 → preview 5.2 → finestra 5.3 → rollback 5.4). Serve **D2** (finestra) e, nella sessione, l'ok esplicito del titolare per: impostare le env su Vercel, pushare `v2`, leggere/migrare il DB di produzione, promuovere. Stato al 2026-09-21: V2-5a chiusa (migrazione finale pronta, staging verde), `v2` non ancora pushato dopo V2-1; `main` resta v1 in produzione.

Cose da sapere ereditate da A-2:
- **Il motore fiscale non è revisionato.** Finché D6 è aperta, il flag `fiscal_engine` non va acceso su un'impresa che poi userebbe quei numeri per versare. La procedura per chiudere la revisione (riportare le risposte nelle regole, compilare i 5 casi golden) è in fondo a `docs/compliance/REVISIONE-COMMERCIALISTA.md` e in RUNBOOKS §7.1.
- **Regole per anno**: a gennaio si scrive `lib/config/src/fiscale/regole/<anno>.ts` e si aggiunge al registro; **non si tocca l'anno precedente**, perché una dichiarazione si rifà con le regole di allora.
- **Permessi**: esiste ora un livello `none` nella matrice di `requirePermission.ts`, usato dall'area `fiscale`. Una nuova area che debba essere chiusa a qualche ruolo si scrive così.

Cose da sapere ereditate da V2-6 (pre-cutover):
- **Marcatura IA**: ogni nuovo generatore di PDF deve passare da `pdfInfo(base, provenienza)` (`lib/pdfmake.ts`) e `qa:pdf` lo verifica per prefisso di file (`quote-*` = ai, `contract-*` = ai_assisted, altro = none). Nuovi canali conversazionali con IA devono mostrare l'avviso al primo contatto (chiavi `supportBot.aiNotice`, `assistant.aiNotice`, costante `AI_DISCLOSURE` in `routes/whatsapp.ts`).
- `STATIC_SEO_RE` in `preventivo-ai/src/main.tsx` decide quali pagine prerenderizzate NON vengono ri-renderizzate da React: se si aggiunge un prefisso di landing statica va aggiunto lì, altrimenti CLS.
- `qa:visual` e `qa:lighthouse` richiedono Chrome installato; sotto Git Bash gli argomenti che iniziano con `/` vanno passati con `MSYS_NO_PATHCONV=1`.

Cose da sapere ereditate da V2-5a:
- **Migrazione finale** = `migrations/v2/0001_v1_to_v2_additive.sql` (43 tabelle, 53 colonne, 45 FK, 69 indici, 1 enum; 409 ms; idempotente). Rigenerata per trasformazione deterministica della 0001 di V2-3, verificata con `pg_dump --schema-only` identico allo staging V2-4, drift 0, e2e 60/60. `0002` non esiste più.
- **Riconciliazione**: `docs/sql/reconcile.sql` (24 conteggi + 6 checksum) da lanciare prima e dopo la migrazione e confrontare con `diff`. Sul baseline vs staging migrato i checksum coincidono; differiscono solo `auth_verification`/`email_events` perché la suite e2e ha scritto sullo staging dopo la migrazione — in prod, con v1 live, possono cambiare i conteggi delle tabelle "vive" (sessioni, verifiche, preventivi) fra prima e dopo; i checksum vanno letti a parità di righe.
- **Env nuove per la prod** (tabella in RUNBOOKS §5.1): `BETTER_AUTH_URL`, `TRUSTED_ORIGINS`, `CRON_SECRET`, `TOKEN_ENCRYPTION_KEY`, `STRIPE_CONNECT_WEBHOOK_SECRET`, facoltative `OPS_ALERT_EMAIL`/Sentry. Bucket Storage: i default (`public-assets`/`private-assets`) sono gli stessi di v1.
- **Preview v2 con env Production** legge il DB prod *non migrato* finché non si esegue 5.3: usarla solo in lettura prima della finestra.
- Lo staging locale è ora sul baseline + nuova 0001 (con i residui della suite e2e): resettare dal template prima di altri confronti.

Cose da sapere ereditate da V2-4:
- **Migrazione**: ~~da rigenerare~~ fatto in V2-5a (vedi sopra).
- **Env Vercel per V2-5**: l'inventario in `ENV-INVENTORY.md` è rigenerato senza le variabili delle integrazioni rimosse; `INCENTIVES_SOURCE_FETCH` è solo locale/e2e (non impostarla in prod).
- **Widget v1** (`public/widget.js`, identico a v1, installato sui siti dei clienti): chiama `GET /api/public/incentives` e `POST /api/public/quotes/:id/incentives` — contratto preservato in `routes/incentives.ts`, calcolo in `incentives/calc.ts` (test a parità con i `client_data.incentivesData` reali). Non cambiare la forma delle risposte.
- **clientData**: i preventivi v1 hanno le chiavi `citta/cap/provincia/codiceFiscale/incentivesData`; il codice v2 legge via `readQuoteClientData()` (lib/db) e scrive le chiavi v2. Non fare una migrazione dati sul JSON.
- **Pro-forma**: numerazione `PF-anno-n` / `NC-anno-n` (sequenze per tipo in `invoice_sequences`), avviso art. 21 DPR 633/72 su PDF/email/pagina pubblica. Le tabelle fatture sono vuote in prod: nessun dato da migrare.
- Il cron giornaliero ora esegue la verifica AI del catalogo incentivi (v1) e chiude i bandi scaduti: costa una chiamata AI al giorno (10 bandi). Bandi placeholder del seed = quelli già in prod (10 righe, `human_verified=false`): da far verificare a mano dall'admin (tab "Incentivi").
- Dati storici: un preventivo widget in prod ha `totale = 0` con capitoli valorizzati (`96660e75…`): dato v1, non un bug v2.
- Screenshot del browser integrato: restituisce immagini bianche quando la finestra è in background — verificare con `get_page_text`/`read_page`.

Cose da sapere ereditate da V2-3:
- **Staging locale**: Postgres 17 in `C:\Users\Admin\PrevAI-staging\` (porta 5433, TLS on), DB `prevai_staging` + copia pristina `prevai_v1_baseline` per il reset; worktree `v1\` con il codice `v1-final` buildato. Avvio/reset/comandi in `docs/RUNBOOKS.md` §3. Se il cluster non è avviato: `pg_ctl … start` (RUNBOOKS §3). Lo staging contiene 2 preventivi di prova creati da v1 (`N° 49.2026` manuale + uno AI), un utente `v23-signup@e2e-test.invalid` e una `whatsapp_sessions`: resettare dal template prima di nuove riconciliazioni.
- L'app v2 gira in locale contro lo staging: `api-server-staging` (5050, legge `.env.staging`) e `preventivo-ai-staging` (5175, proxy → 5050) da `.claude/launch.json` della cartella padre; oppure `node --env-file=.env.staging artifacts/api-server/dist/index.mjs` dopo `pnpm --filter @workspace/api-server run build`. Per entrare come utente reale: cookie di sessione firmato (RUNBOOKS §3). Nota: sulla porta 5000 gira spesso un processo `walkthrough.ts` di QuoteAI da altre sessioni — non toccarlo.
- `lib/db/src/index.ts`: `?sslmode=disable` in `DATABASE_URL` spegne TLS (solo per Postgres locali); in prod nulla cambia.
- Suite e2e: `pnpm --filter @workspace/api-server test:e2e` legge `.env.staging`; senza `SUPABASE_URL` usa lo storage in memoria (`E2E_STORAGE_STUB=1`). Dati ora italiani (province `MI`/`NA`/`RM`, EUR, `preferredLanguage: "it"`, oggetti email italiani; un oggetto inglese/francese fa fallire il test "template hygiene"). Nuovo script `qa:historic-pdf` per rendere PDF di preventivi reali.
- **Colonne canadesi ancora nello schema v2** (e quindi nella migrazione 0001): `business_profiles.gst_hst_number/pst_number/qst_number/etransfer_email/homestars_profile_url/licence_number`, `incentives_catalog.province/city/income_tested` + default `'federal'/'all'/'rebate'`, `collaborators.role` default `'worker'`, `quotes/projects/clients.province` (ora sigla italiana, ok). V2-4 li toglie/riallinea (incentivi v1 ripristinati); poi in V2-5 si **rigenera** `migrations/v2/0001` dallo schema finale con il procedimento di RUNBOOKS §4.
- `resolveQuoteTaxRate` (api-server `lib/tax.ts`) torna a 22 % quando l'AI non indica un'aliquota: in QuoteAI la provincia decideva l'aliquota e senza provincia dava 0 % — sarebbe stato un bug su ogni preventivo AI senza provincia nel profilo.
- Il PDF preventivo v2 aveva ancora "Unit Price ($)"/"Total ($)" nelle intestazioni tabella (`quotes/pdf.ts`): corretto. L'audit i18n non copre i template PDF backend: in V2-6 aggiungere un controllo sui PDF (`qa:pdf` + grep).
- Dati storici: alcuni preventivi v1 hanno `titolo_preventivo_riga2` con il placeholder letterale "[Comune] ([Prov])" (l'AI v1 non lo sostituiva). Non è un problema di migrazione; valutare in V2-4 se ripulire il prompt (`generateQuoteFromText.ts` riga ~63 ha lo stesso esempio) e i dati.

Cose da sapere ereditate da V2-2:
- Env: il backend legge `PREVAI_BASE_URL` (rinominata da `QUOTEAI_BASE_URL` in V2-2e; `.env`/`.env.production` v1 la usano già). `BETTER_AUTH_URL` resta prioritaria.
- I piani Stripe in `api-server/src/routes/payments.ts` sono tornati agli id/prezzi EUR di PrevAI v1 (Starter 19, Pro 49, Elite 59, singoli 3/9). Le feature "team", "cantieri", "fatture" di QuoteAI non sono ancora mappate ai piani: si decide in V2-4 (D5).
- `pnpm --filter @workspace/preventivo-ai i18n-audit` è ora l'audit invertito (fallisce su chiavi mancanti, valori del dizionario in inglese, split core/dashboard, letterali inglesi nel JSX app); gira in CI (`test.yml`). `pnpm run spell` usa il dizionario `it-it` + lista parole in `cspell.json`.
- L'harness e2e (`api-server/src/e2e`, `vitest.e2e.config.ts`) contiene ancora dati canadesi (province ON/QC, HST): non gira senza DB, si sistema in V2-3 quando esiste lo staging.
- `jobs/dates.ts` usa `Intl.DateTimeFormat("en-CA")` di proposito (dà `YYYY-MM-DD`), non è un residuo.

## Come è stata avviata V2-2 (istruzioni originali)

Apri una sessione nella cartella `C:\Users\Admin\Downloads\PrevAI (2)\PrevAI`, `git checkout v2`, e scrivi: **"Leggi docs/PIANO-AZIONE.md ed esegui la fase V2-2"**. Dettagli in `PREVAI-V2-PLAN.md` §4 V2-2. Prima di iniziare serve la decisione **D1** (design system). Il codice v1 da cui recuperare `seo-data.ts`, blog, prompt AI e incentivi è nel tag `v1-final` (`git show v1-final:artifacts/preventivo-ai/src/…`).

Note operative ereditate da V2-1:
- I comandi locali vanno lanciati con i filtri per **nome pacchetto** (`--filter @workspace/preventivo-ai`), mai per path (`./artifacts/**`): le parentesi in `PrevAI (2)` rompono il glob di pnpm.
- Il repo è `eol=lf` via `.gitattributes`: i 3 hash CSP in `vercel.json` sono calcolati sull'`index.html` LF. Dopo aver toccato gli script inline di `index.html`: `pnpm --filter @workspace/api-server csp-hashes`.
- `.env` locale è ancora quello v1 (Groq ecc.): l'app v2 non gira ancora in locale contro un DB; lo farà su `prevai-staging` in V2-3. Vedi `docs/ENV-INVENTORY.md` (sezione QuoteAI) per le variabili nuove.
- `.claude/launch.json` locale: `preventivo-ai` (:5173) e `api-server` (:5000).

## Storico: come è stata eseguita V2-1

1. Export di QuoteAI `HEAD` (`5d65f61`, Phase 71) con `git archive` — repo QuoteAI non toccato. I 18 file **non committati** di QuoteAI ("Phase 72": leave-team, 2FA UI, privacy) **non** sono stati importati.
2. Rinomina `artifacts/quote-ai` → `artifacts/preventivo-ai` e `quote-ai` → `preventivo-ai` in tutti i riferimenti (package name, `vercel.json`, `pnpm-workspace.yaml`, lockfile, `knip.json`, `eslint.config.mjs`, `cspell`, CI `.github/workflows/*`, script, docs).
3. Albero v1 tolto dal branch (recuperabile dal tag `v1-final`); tree QuoteAI copiato; `docs/RUNBOOKS.md` e `docs/ENV-INVENTORY.md` di PrevAI mantenuti in testa con la versione QuoteAI accodata come sezione; altri docs QuoteAI importati (DESIGN-SYSTEM, ROUTE-MATRIX, QA-VERIFICATION-PLAN, …). I 5 piani PrevAI restano.
4. `pnpm install --frozen-lockfile`, `pnpm run typecheck` (pulito), build api-server + preventivo-ai (428 pagine prerender, ancora canadesi), `pnpm test` 65/65, lint 0 errori, i18n-audit identico al baseline QuoteAI. Knip non eseguibile in locale senza `DATABASE_URL` (carica `drizzle.config.ts`). E2e non eseguiti (richiedono DB: V2-3).

## Storico: come è stata eseguita V2-0

Apri una nuova sessione nella cartella `C:\Users\Admin\Downloads\PrevAI (2)\PrevAI` e scrivi: **"Leggi docs/PIANO-AZIONE.md ed esegui la fase V2-0"**. La fase V2-0, nell'ordine:

1. ~~D0 e pulizia remote~~ — **già fatto il 2026-09-21** (origin pulito, auth verificata). Passare al punto 2.
2. Triage dei 101 file non committati su `main` (`git status`, `git diff --stat`): raggrupparli per tema (catalogo, public-quotes, schema `quotes.ts`, home, SEO, ~85 OG blog, `pages/p/`, mockup preview), spiegarli al titolare (D4), committarli per tema o scartarli. Non aprire `v2` prima che `main` sia pulito.
3. `pg_dump` baseline della produzione via pooler session-mode (porta 5432, utente `postgres.<ref>`, password URL-encoded), formato custom, salvato **fuori dal repo**; annotare in `docs/RUNBOOKS.md` (creare il file) dimensione, sha256, conteggi per tabella; verificare `pg_restore --list`.
4. `vercel env ls production` sul progetto `prevai` → `docs/ENV-INVENTORY.md` (solo nomi).
5. `git tag v1-final` su `main`; `git checkout -b v2`; push di tag e branch.
6. Aggiornare la tabella di stato qui: riga 1 → ✅ con data e commit. Chiudere la sessione o proseguire con V2-1.

## Diario

- **2026-09-22 (A-2, prima del cutover)** — Il titolare ha chiesto "fase 10" con **D6 ancora aperta**, cioè con la regola scritta in `REVISIONE-COMMERCIALISTA.md` che dice "A-2 parte solo con F1–F16 tutte ✅". Scelta fatta, e messa per iscritto prima di iniziare: si esegue A-2 per intero come per A-0 (D2 aperta) e A-1 (D8 aperta), ma **ogni regola nasce `non_revisionata`** e il prodotto non può presentare un numero come verificato. Il freno è in tre punti indipendenti: `Calcolo.revisionato` è false e porta l'elenco delle regole non confermate; ogni risposta API include `revisione` regola per regola; la pagina mostra l'avviso in cima e non lo nasconde mai. Sei sotto-tappe su `v2`. **a** motore in `@workspace/config` (non un pacchetto `lib/fiscal` nuovo: `config` è già dependency-free e importabile da frontend, server e test, e `iva.ts` ci teneva già dei calcoli): `regole/2026.ts` con soglie, aliquote 15 %/5 %, INPS artigiani e commercianti, riduzioni 35 %/50 %, acconti e scadenze, e le 16 regole con fonte e stato di revisione; `ateco.ts` col coefficiente per **gruppo** e non per codice esatto, così un codice nuovo della ATECO 2025 dentro un gruppo noto continua a rispondere bene; `calcolo.ts` puro (nessuna data di sistema letta di nascosto: anno e mese arrivano dall'ingresso, così i golden sono riproducibili fra anni); `golden.ts` coi 5 casi della checklist. **b** `tax_profiles` e `fiscal_payments`, migrazione `0004` additiva. **c** raccolta dati che **non duplica nulla** (incassi da `invoice_payments`, maturato dalle fatture aperte, pipeline per differenza fra accettato e fatturato), servizio, 9 rotte, monitor della soglia nel cron. **d** pagina `/dashboard/fisco`. **e** 13 test e2e. **f** correzioni trovate provandola a video. Verifica: typecheck 0, lint 0 errori (69 warning pre-esistenti), unit 120, **e2e 92/92**, i18n-audit 0, cspell 0, build con 341 pagine prerenderizzate, route matrix 333, migrazione su staging in 0,30 s con drift 0, pagina aperta nel browser contro lo staging con dati veri (64.000 € incassati → imponibile 53.909,66 €, imposta 8.086,45 €, soglia "vicino" a 76.000 su 85.000 con proiezione 88.000, simulatore su 15.000 € → restano 9.840 € e compare l'avviso di sforamento). Decisioni e scoperte: (a) **la base INPS non è la base fiscale** — i contributi si calcolano sul reddito forfettario senza dedurre i contributi versati, l'imposta su quello stesso reddito meno i contributi: dedurli due volte sarebbe un errore a favore dell'utente, che se ne accorgerebbe solo a saldo. Aggiunto alla riunione col commercialista come domanda esplicita (F6/F10), insieme a scaglione e massimale INPS 2026, che sono ricavati per proporzione e non letti dalla circolare; (b) **nuova area di permessi chiusa**: `fiscale` è la prima area in cui un ruolo non ha nemmeno la lettura (office, foreman, viewer), perché fino ad A-2 ogni ruolo poteva almeno guardare tutto e per la posizione fiscale del titolare non va bene — ha richiesto un livello `none` nella matrice; (c) il simulatore **ricalcola due volte** invece di applicare un'aliquota marginale a mano: così scalini INPS, minimale e soglie compaiono da soli e non c'è un secondo posto dove sbagliare; (d) la pipeline è una stima per differenza (non esiste un aggancio preventivo→fattura per i lavori entrati a mano) e sovrastima apposta, perché il monitor deve avvisare prima e non dopo; (e) trovati a video due difetti di formattazione che i test non vedevano — percentuali col punto decimale e importi `toFixed(2)`. **Non fatto (di proposito)**: F24 precompilati per imposte e contributi e scadenzario unificato col bollo (sono A-3), prima nota e quadro LM (A-4), prezzo dell'add-on (A-5), e qualunque forma di consiglio su quale riduzione contributiva scegliere. **Durante la sessione un'altra sessione ha committato `e54ff014e` (A-1h) sullo stesso branch**: nessun conflitto, ma vale la regola "un agente alla volta". Prossimo: V2-5 b/c (cutover, D2); per rendere usabili i numeri di A-2, **D6**.

- **2026-09-22 (A-1, prima del cutover)** — Il titolare ha chiesto "fase 9" con D2 e D8 ancora aperte, e ha confermato: **Openapi come intermediario di default** con adapter + simulatore (nulla parte verso lo SdI senza contratto firmato) e **gating a flag per organizzazione**. Cinque sotto-tappe su `v2`. **a** `lib/config/src/fatturapa.ts`: tipi documento, regimi fiscali, nature, modalità di pagamento, mappa `IvaCode` → aliquota/natura/riferimento normativo (nel forfettario ogni riga diventa N2.2 qualunque cosa dica il documento), controlli formali su P. IVA (checksum), C.F. (con omocodia), codice destinatario, PEC, CAP, IBAN (mod 97), progressivo e nome file, 45 codici di scarto tradotti; schema `sdi_settings`/`e_invoices`/`e_invoice_events`/`supplier_e_invoices`/`bollo_periods`, `clients` con C.F./codice SDI/PEC/CIG/CUP, `business_profiles.city/cap`, feature `sdi_invoicing` come add-on fuori da ogni piano; migrazione `0003` additiva. **b** generatore FatturaPA 1.2.2 scritto a mano (l'ordine degli elementi è parte dello schema) + mapper: la **ritenuta a garanzia diventa una riga negativa** (altrimenti il riepilogo non torna con le righe → scarto 00420) e la **nota di credito TD04 porta importi positivi** (in PrevAI sono negativi per applicarsi da sole alla fattura originale); 25 controlli pre-invio con il codice di scarto accanto; 7 test golden. **c** adapter (`simulato` pilotato dal codice destinatario, `openapi` con rotte in un solo blocco da riconfrontare all'attivazione), servizio con XML archiviato **anche nel nostro storage** prima della consegna, eventi idempotenti, notifiche di scarto/mancata consegna/rifiuto; bollo ricalcolato dalle trasmissioni (uno scarto non lascia residui); ciclo passivo; parser della FatturaPA in arrivo senza dipendenze. **d** rotte del modulo, webhook `POST /api/webhooks/sdi/:userId` col segreto per impresa confrontato in tempo costante e corpo trattato come dato, manutenzione nel tick del cron (stati non arrivati via webhook, passive, bollo + promemoria una volta per trimestre). **e** interfaccia: pannello sulla fattura (controlla → invia → ricevute → XML), scheda Impostazioni con i sei passi e il codice destinatario da registrare in Fatture e Corrispettivi, pagina Amministrazione (casella fornitori + bollo con F24 da ricopiare). **Decisione di prodotto**: col modulo attivo la fattura nasce nella serie **FT-** con `invoices.fiscale = true`, e PDF/email/pagina pubblica smettono di dire "pro-forma" per dire "copia di cortesia" (una pro-forma con un XML valido alle spalle sarebbe una bugia al cliente). Verifica: typecheck 0, lint 0 errori (69 warning pre-esistenti), unit 79, **e2e 79/79** con `sdi.e2e.test.ts` (13 casi: modulo spento, onboarding, credenziali cifrate che non tornano indietro, emissione, scarto e rinvio, bollo, ciclo passivo, webhook), build verde, cspell 0, route matrix 324, migrazione 0003 su staging in 0,26 s con drift 0. Scoperte: (a) la colonna `sdi_settings.stato` come unica verità era sbagliata — completare i dati fiscali dal profilo aziendale la lasciava indietro e si continuavano a emettere pro-forma: lo stato ora si **ricalcola dai requisiti** (`sdi/stato.ts`) e la colonna resta una cache; (b) lo sweep IDOR e la route matrix vanno insegnati a ogni rotta nuova (webhook con parametro nel path, anno/trimestre che non sono identificatori di nessuno); (c) il `Write` interpreta gli escape `\uXXXX`, quindi una classe di caratteri di controllo scritta come letterale finisce nel file come byte veri: costruita da stringa. **Non fatto (di proposito)**: addebito del bollo al cliente in fattura (oggi l'impresa lo assolve e non lo ribalta: eviterebbe divergenze fra PDF e XML, da valutare in A-4/A-5), adapter A-Cube (in attesa del preventivo), invio automatico senza conferma (vietato dalla regola di prodotto §7). Prossimo: V2-5 b/c (cutover, D2) e, per rendere emettibile il modulo, D8.

- **2026-09-21 (A-0, prima del cutover)** — Il titolare ha chiesto "fase 8" con D2 ancora aperta: A-0 eseguita su `v2` prima di V2-5 b/c, con l'unica modifica di schema in `migrations/v2/0002_a0_compliance.sql` (additiva, da eseguire dopo la 0001: RUNBOOKS §5.3 aggiornato). **2FA obbligatoria**: colonna `business_profiles.two_factor_required`; `resolveActingOrg` la restituisce e `requireAuth` risponde **403 `two_factor_required`** a chi non ha la 2FA su tutte le rotte tranne le 4 che servono alla schermata di blocco (`GET /api/security/policy`, `GET /api/business-profile`, `GET /api/team/orgs`, `POST /api/team/switch`); `PATCH /api/security/policy` owner-only, rifiuta l'attivazione se il titolare stesso non ha la 2FA (400 `two_factor_not_enabled`), audit `two_factor.policy_enabled/disabled`; dashboard: card "Verifica in due passaggi obbligatoria" nella tab Sicurezza (toggle solo per l'owner) e `TwoFactorGate` al posto del contenuto in `DashboardLayout` quando la policy non è soddisfatta (riusa `TwoFactorCard`). Il modulo Amministrazione la imporrà (`twoFactorLocked`, oggi sempre false). **Cifratura campi fiscali**: `lib/fieldCrypto.ts` (AES-256-GCM sotto `TOKEN_ENCRYPTION_KEY`, prefisso `enc1:`, lettura trasparente delle righe in chiaro pre-A-0) applicata a `business_profiles.iban` in scrittura (`PUT /business-profile`) e lettura (profilo, pro-forma); script idempotente `ops:encrypt-fiscal-fields` per il pregresso (**da lanciare dopo il promote**, non prima: il codice vecchio mostrerebbe il cifrato); `rotate-token-key.ts` ora conosce le colonne con prefisso. Il C.F. resta in chiaro (stampato su ogni documento, serve alla FatturaPA): annotato in DPIA §7. **Legale**: Termini §9 = accordo art. 28 utente↔PrevAI (oggetto, istruzioni, sub-responsabili con preavviso 14 gg, notifica violazioni 48 h, cancellazione/restituzione, obblighi dell'utente), §7 trasparenza IA art. 50, §3 policy 2FA; Privacy §2 (ruoli) e §8 (cifratura, policy, DPIA). **Documenti** in `docs/compliance/`: DPIA (WP248: 12 rischi, misure v2 + A-0..A-6, residuo basso, riesame per fase), registro art. 30 (T1–T7 titolare, R1–R7 responsabile), tabella fornitori/DPA (priorità: DPA Groq con no-training, regione funzioni Vercel), pacchetto per il commercialista (F1–F16 + 5 casi golden, bloccato da D6). Verifica: typecheck 0, unit 72/72, **e2e 66/66** (nuovo `compliance.e2e.test.ts`), build ok, cspell 0, lint 0 errori (69 warning pre-esistenti), route matrix 307, drift staging 0, script cifratura provato su staging (1 riga → `enc1:`, rieseguito = 0), rotazione chiave dry-run ok, smoke in browser (gate e toggle, audit `two_factor.policy_enabled`). Scoperte: (a) il cookie better-auth è HttpOnly: `document.cookie` non lo sovrascrive, ma lo staging aveva già la sessione giusta; (b) sul dev server Vite la prima navigazione dopo nuovi import lascia la pagina vuota per decine di secondi (nessun errore), la seconda va: non è un bug applicativo. Prossimo: V2-5 b/c (cutover, D2), poi V2-6 d; A-1 richiede D8.

- **2026-09-22 (V2-6, parte pre-cutover)** — Eseguita prima di V2-5 b/c (in attesa di D2) la parte di V2-6 che non richiede la prod. **AI Act art. 50**: `pdfInfo()` in `lib/pdfmake.ts` scrive `Creator/Producer = PrevAI` e, per provenienza, `Subject`+`Keywords` ("AI-generated" sui preventivi, "AI-assisted" sui contratti perché oggetto/tempi sono bozze IA riviste, niente sulle fatture); `quoteProvenance()` in `quotes/pdf.ts` = IA a meno di `rawInput` "[Manual quote]"; riga a piè di pagina "contenuto elaborato con intelligenza artificiale e verificato dall'impresa" sui preventivi definitivi (le bozze mantengono DOCUMENTO PROVVISORIO); avviso nel piè dell'email di invio preventivo (`aiGenerated`) e nella pagina pubblica `/p/:id` (campo `aiGenerated` nella risposta pubblica, fuori dall'OpenAPI); avviso in-context nel support bot (sotto il saluto, finché non subentra un operatore), nell'assistente cantiere (`.chat-notice` sotto l'intestazione) e nel bot WhatsApp (`AI_DISCLOSURE` a ogni inizio sessione e al collegamento account). **QA**: `qa:pdf` ora rilegge ogni PDF con pdf-parse e fallisce su residui inglesi o marcatura IA mancante/indebita (44/44 verdi; scoperto che `getText()` e `getInfo()` in parallelo sullo stesso parser falliscono con "Cannot transfer object" — vanno in sequenza); `qa:visual` 104 pagine × it a 1280/375: 2 violazioni axe serious (`.table-wrap` scrollabile senza tastiera su fattura pubblica/dashboard) corrette con `tabindex=0`+`role=region`+focus ring nei render server (fatture e contratti); le rotte pubbliche della sweep erano ancora canadesi (`/quotes/painter/toronto`, `/fr/soumissions`, blog canadese → tutte 404): sostituite con le rotte italiane reali. **Lighthouse** (mobile, 2 run): la pagina città `/preventivi/imbianchino/bergamo/` aveva **CLS 0,82** e perf 70 perché `STATIC_SEO_RE` in `main.tsx` matchava ancora `/quotes/…` e `/fr/soumissions/…` invece di `/preventivi/…`, quindi sulle landing italiane veniva montata l'intera App sopra l'HTML statico: regex corretta → CLS 0, perf 92, JS 227→89 kB su tutte le landing. Restano perf 88 (LCP 3,5 s su Moto G/4G lenta) su `/` e `/help/` e a11y 94 su blog/settore (target 90 del piano: da rivedere dopo il cutover su CDN Vercel). **SEO**: i 330 URL della sitemap v1 sono tutti prerenderizzati in v2 con canonical identico (`https://prevai.it/…`), `<html lang="it">`, nessun noindex, hreflang solo `it-IT`+`x-default` (v1 usava `it`); il `dist/` committato in `v1-final` ha canonical `www.prevai.it` ma è una build vecchia: il sorgente v1 ha `BASE_URL = https://prevai.it`, coerente con la sitemap. Controllo copertura GSC non fatto (serve accesso GSC; script `gsc-inspect-urls.ts` disponibile). Verifica: typecheck 0, lint 0 errori, unit 72, e2e 60/60 (staging resettato dal template + 0001), build 341 pagine, validate-prerender/sitemap verdi, i18n-audit 0, cspell 0, route matrix rigenerata (305 route). Nessuna modifica di schema. Prossimo: V2-5 b/c (cutover) poi V2-6 d.

- **2026-09-21 (V2-5a, preparazione)** — `migrations/v2/0001` rigenerata dallo schema finale di V2-4: script deterministico sulla 0001 di V2-3 (via le 11 tabelle canadesi con FK/indici, `business_profiles` con le 5 colonne italiane al posto delle 6 canadesi, `incentives_catalog` intoccata, `leads` senza `google_lsa_*`, e-Transfer → bonifico, `contract_signers.tax_id`, intestazione riscritta). Verifica: staging resettato da `prevai_v1_baseline` → 0001 in 409 ms, rieseguita = no-op → `pg_dump --schema-only` identico allo staging V2-4 (solo ordine colonne) → drift 0 fatali → e2e 60/60. `0002_v2-4_staging_delta.sql` eliminata. Scritto il runbook di cutover in RUNBOOKS §5 (env Vercel da aggiungere, preview, finestra passo-passo con comandi, rollback = promote del deployment v1, nessuna azione sul DB) e `docs/sql/reconcile.sql` (conteggi + checksum, testato baseline vs migrato). Nessun codice applicativo toccato. Prossimo: D2 + ok del titolare per env Vercel, push `v2`, migrazione prod, promote.

- **2026-09-22 (V2-4)** — D3 confermata dal titolare (tabella così com'è, tabelle delle integrazioni rimosse; gating piani da Pro). **a**: QuickBooks, Wave, Flinks, Financeit, Google LSA fuori da route, servizi, automazioni, cron, webhook, schema Drizzle, feature dei piani, Impostazioni, pagina pubblica, client API, OpenAPI+codegen, 114 chiavi i18n, e2e, manifest env; route matrix rigenerata (302 route, 45 file). **b/d**: `business_profiles` con `codice_fiscale`, `codice_sdi`, `rea_number`, `iban`, `secondary_review_url` al posto di GST/HST-QST-PST-licence-e-Transfer-HomeStars (P. IVA resta `vat_number`); `collaborators.role` default `collaboratore` come in prod; parti e istruzioni di pagamento delle fatture in termini italiani (`bank_transfer`, IBAN, segnalazione bonifico); documenti emessi come **pro-forma** (PF-/NC-, avviso art. 21 su PDF A4, email, pagina pubblica, dashboard). **c**: schema `incentives_catalog` = prod (statale/regionale/comunale, regione/comune, default v1); endpoint del widget v1 con contratto identico; calcolo estratto in `incentives/calc.ts` con test sui numeri reali dei preventivi in prod (parità v1, con una miglioria: il bando comunale dal CAP vince su quello regionale, come nei dati); matching italiano per `/p/:id`; seed del catalogo; verifica AI v1 sul tick cron con chiusura automatica dei bandi scaduti; box "Agevolazioni fiscali stimate" nei PDF; admin catalogo in italiano; `readQuoteClientData()` per le chiavi v1 del JSON cliente; etichette PDF cliente C.F./P. IVA. **e**: `contract_signers.tax_id` (C.F./P. IVA facoltativo, validato) nella pagina di firma e nel PDF; consensi con riferimento a Reg. UE 910/2014 e art. 20 CAD; commenti CASL→GDPR; prompt AI senza "[Comune] ([Prov])" + `stripLocationPlaceholder`. Verifica: typecheck 0, lint 0 errori, 72 unit, build 341 pagine, i18n-audit 0, cspell 0, staging con delta 0002 → drift 0, **e2e 60/60** (nuovo `incentives.e2e.test.ts`), smoke in browser (settings azienda/integrazioni, pro-forma, `/p/:id` con card incentivi) e PDF storico con box incentivi. Scoperte: (a) `drizzle-kit push` chiede conferma interattiva sulle colonne rinominate anche con `--force` — delta SQL a mano; (b) la verifica incentivi v1 con AI non raggiungibile non aggiornava mai `lastCheckedAt` (array vuoto di default): corretto; (c) `it-IT` non raggruppa le migliaia sotto 5 cifre anche nei testi degli incentivi (~€5689), identico a v1; (d) la QuoteAI `RebatesWidget` usava `province`/`incomeTested` che in Italia non esistono: ora regione da `regioneDiProvincia` e ISEE massimo.

- **2026-09-21 (V2-2 a+b)** — Creato `lib/config` (`@workspace/config`: MARKET EUR/it-IT, regime IVA 22/10/4/RC/SP/esente, bollo, 107 province + regioni, formattazione, riferimenti legali). `lib/db` `tax.ts` ri-esporta da config; enum lingua `it`; default preventivo tornati a v1 (titolo, nota, IVA 22). Backend: documenti, contratto d’appalto italiano (c.c., Cod. Consumo, eIDAS), email, follow-up, prompt assistente/cantiere in italiano; `TaxProfile` per regime in OpenAPI. Frontend: `Lang = "it"`, dizionari `fr` eliminati (testi ancora inglesi sotto `it`), route `/fr` rimosse, toggle lingua tolto, date-fns `it`, province italiane nei selettori, builder manuale con regime IVA. SEO: dati v1 (seo-data, intelligence, engine, blog 78 articoli) rimessi con slug identici; i 330 URL della sitemap v1 sono tutti prerenderizzati (+11 help). Build/typecheck/lint/test verdi a ogni sotto-tappa. Scoperte: (a) gli heredoc bash con `$VAR` non definito bloccano il tool — usare file di script; (b) `it-IT` non raggruppa le migliaia sotto 5 cifre (`1234,50`), è CLDR-corretto; (c) le pagine settore/città prerenderizzate di QuoteAI usano ancora il markup Tailwind v1, quindi le funzioni v1 (`buildQuantoCostaBlock`) si innestano senza adattamenti.
- **2026-09-21 (V2-3)** — Titolare sceglie lo **staging locale** (Postgres 17 su questa macchina) invece di un terzo progetto Supabase. Restore del dump baseline: 24 tabelle, conteggi identici. Migrazione `migrations/v2/0001_v1_to_v2_additive.sql` generata con drizzle-kit pull+generate e resa additiva a mano (trasformazione: `IF NOT EXISTS`, guardie `duplicate_object`, scartati 2 DROP COLUMN e 7 SET/DROP DEFAULT canadesi, `unsubscribe_token` con default DB): **342 ms**, idempotente, drift 0 fatali. Suite e2e portata da 30 fallimenti a **63/63** senza toccare il DB: stub Storage in memoria (`storageStub.ts`), `sslmode=disable` nel client pg, harness italianizzato (province MI/NA/RM, EUR, lingua `it`, brand PrevAI, piani Stripe v1, test Phase 71 riscritti sul regime IVA). Smoke v2 in browser con la sessione reale di un utente (48 preventivi, dashboard, dettaglio con 5 capitoli) e PDF di 5 preventivi storici con `qa:historic-pdf`. Smoke v1 (worktree `v1-final`, TLS locale) sul DB migrato: sessione, lista, storico, manuale, AI Groq, sign-up, webhook WhatsApp firmato — tutto ok; v2 legge i preventivi scritti da v1. Checksum quotes/users identici al baseline (normalizzando `iva_percentuale`). Bug trovati e corretti: intestazioni PDF "Unit Price ($)"/"Total ($)"; data dettaglio preventivo in formato ISO; `resolveQuoteTaxRate` che dava 0 % senza provincia. Scoperte: (a) drizzle-kit generate chiede conferma interattiva sulle rinomine — serve lo shim TTY + `\r` in pipe; (b) le sequenze `\\` e `\b` passate a `node -e` dal tool Bash vengono alterate: script su file; (c) better-auth firma il cookie con HMAC-SHA256 in base64 standard (non url-safe).
- **2026-09-21 (V2-2 c–f, chiusura)** — **c/d**: `translations.ts` e `translations.dashboard.ts` tradotti integralmente (3104 chiavi sotto `it`), help-centre, home, stats bar, testimonial, termini/privacy v1 (Cod. Consumo, GDPR), placeholder e valute; `seo-slugs.ts` con mappa slug QuoteAI→v1. **e**: un solo prompt AI in `generateQuoteFromText.ts` (base v1: prezzi italiani 2026 e adeguamento Nord/Centro/Sud + struttura QuoteAI: listino prioritario, misure vincolanti, zero omissioni, documento 1:1, descrizione generale, note con validità/esclusioni/garanzia art. 1667 c.c.); `routes/quotes.ts` e il widget lo importano; percorsi deterministici a IVA 22 e rate 30/30/30/10; bot WhatsApp, OCR listino, estrazione documenti, support bot, assistente cantiere, notifiche di automazioni/fatture/varianti ed email di reset/verifica tutte in italiano ed EUR; piani Stripe v1; `cspell` su `it-it`; 5 refusi nel blog v1 corretti (superfice, Rifacere, lesiniare, Autolitante, capriati). **f**: `i18n-audit.ts` invertito (0 su tutti i controlli), typecheck 0 errori, lint 0 errori, 64 test, build 341 pagine, validate-prerender/sitemap verdi. Scoperte: (a) il backend leggeva `QUOTEAI_BASE_URL` mentre le env Vercel v1 hanno `PREVAI_BASE_URL` — sarebbe stato un bug di deploy in V2-5, rinominata ora; (b) la "struttura Base/Consigliato/Premium" citata nel piano non esiste nel prompt QuoteAI (le varianti sono generate a parte da `/variants`): il prompt unificato integra le regole 11–14 di QuoteAI, non tre fasce; (c) nel tool Bash le sequenze `\b` dentro `node -e` diventano backspace — le regex vanno scritte in file di script.

- **2026-09-21** — Creati `PREVAI-V2-PLAN.md`, `AMMINISTRAZIONE-PLAN.md` e questo file. Nessun codice toccato. Ricerca di mercato/legale/compliance completata (vedi `AMMINISTRAZIONE-PLAN.md` §13 per le fonti). QuoteAI consultato in sola lettura per ricostruire le 70 fasi.
- **2026-09-21 (V2-0)** — Triage D4: 102 file → 5 commit per tema su `main` (`4265338a5` accettazione pubblica `/p/:id`, `36ad3c965` OCR listino, `063d6f861` home CRM, `d03cf34a8` hero SEO, `dae9d577a` docs); 88 PNG OG scartate (byproduct di build); audit HTML e xlsx Toronto spostati fuori repo. Dump baseline prod (151 KB, 24 tabelle, 106 preventivi, 28 utenti) → `RUNBOOKS.md`. Scoperto che le colonne `accepted_*` erano già in prod. `ENV-INVENTORY.md` creato (13 var su Vercel; Groq è l'unico provider AI). D7 risolta (eu-west-1). Tag `v1-final` = `6dbe45de4`, branch `v2` creato e pushato. Push di `main` ha avviato deploy prod su Vercel.
- **2026-09-21 (V2-1)** — Base QuoteAI (`5d65f61`) importata su `v2` e rinominata `preventivo-ai`. Due scoperte: (a) i filtri pnpm per path non funzionano nella cartella `PrevAI (2)` → script `typecheck` passato ai filtri per nome; (b) `core.autocrlf=true` sulla macchina rendeva CRLF il working tree e faceva fallire il test degli hash CSP → aggiunto `.gitattributes` `eol=lf`. Build verde con `MARKET` canadese: punto di partenza misurabile per V2-2.
- **2026-09-21 (V2-1, deploy)** — Push di `v2` → deploy *preview* Vercel `prevai` (prod intatta). Primo tentativo rotto dalla cache di build v1 (symlink pnpm stale); poi scoperto bug latente ereditato da QuoteAI Phase 61: `kysely` e `@opentelemetry/semantic-conventions` (usati da `@better-auth/core`) erano esclusi dal bundle esbuild ma non risolvibili nella function → fix `b17475932` (ora bundlati). Preview finale: frontend 200 (design QuoteAI, contenuti canadesi come atteso); `/api/*` risponde `BETTER_AUTH_SECRET or SESSION_SECRET must be set` perché le env del progetto Vercel sono ancora quelle v1. **Volutamente non configurate**: puntare l'API v2 al DB di produzione prima di V2-3 violerebbe §1. Le env v2 per preview si mettono in V2-3 (staging) e V2-5 (prod). D1 decisa: design QuoteAI così com'è.
