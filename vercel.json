{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/db build && pnpm --filter @workspace/api-zod build && pnpm --filter @workspace/integrations-openai-ai-server build && pnpm --filter @workspace/api-server build && pnpm --filter @workspace/preventivo-ai build",
  "outputDirectory": "artifacts/preventivo-ai/dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
